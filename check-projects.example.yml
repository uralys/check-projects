# ============================================
# check-projects - Configuration Example
# ============================================
# A fast, cross-platform CLI tool to check the git status
# of multiple projects organized by categories.
#
# Documentation: https://github.com/uralys/check-projects#readme
#
# Setup:
# 1. Copy this file to ~/check-projects.yml or ./check-projects.yml
# 2. Edit the configuration to match your project structure
# 3. Run: check-projects
# ============================================

categories:
  # Mode 1: Explicit project list (using 'projects' field)
  # Use full paths to specific git repositories
  - name: core
    projects:
      - ~/fox
      - ~/cherry

  # Mode 2: Auto-scan directory (using 'root' field)
  # Recursively scans for all git repositories in the directory
  - name: mozilla
    root: ~/Projects/mozilla

  # Mode 2 with ignore patterns
  # Projects listed in 'ignore' will be skipped in this category
  - name: uralys
    root: ~/Projects/uralys
    ignore:
      - my-deprecated-project
      - another-unfollowed-project

# Display options
display:
  hide_clean: true      # Hide projects with ✔ status by default
  hide_ignored: true    # Hide ignored projects from output

# Default behavior options
use_tui_by_default: false  # Set to true to always launch in TUI mode (same as --tui flag)
fetch: true                # Set to true to always fetch from remote before checking status (same as --fetch/-f flag)
